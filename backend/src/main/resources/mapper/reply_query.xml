<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ssafy.chuck.reply.dao.ReplyDao">

	<!-- 댓글 입력 -->
	<insert id="insertComment">
		insert replies(writer, comment, diary_id) values(#{writer}, #{comment}, #{diary_id})
	</insert>
	
	<!-- 특정 아이디의 댓글 리스트 반환 -->
	<select id="selectCommentByDiaryId" resultType="com.ssafy.chuck.reply.dto.ReplyDto">
		select writer, comment, diary_id, date from replies where diary_id = #{diary_id}
	</select>
	
	<!-- 특정 작성자의 댓글 리스트 반환 -->
	<select id="selectCommentByWriter" resultType="com.ssafy.chuck.reply.dto.ReplyDto">
		select writer, comment, diary_id, date from replies where writer = #{writer}
	</select>


</mapper>